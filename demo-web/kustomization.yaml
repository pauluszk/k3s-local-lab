apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: demo

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: demo-web-config
    literals:
      - PODINFO_UI_COLOR=#34577c
      - PODINFO_UI_MESSAGE=Elso verzio - GitOps labor
